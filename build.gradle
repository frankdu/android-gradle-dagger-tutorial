def langLevel = 1.6

allprojects {
    apply plugin: 'idea'
}

idea {
    project {
        jdkName = langLevel
        languageLevel = langLevel
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.9'
}

